apiVersion: v1
kind: Service
metadata:
  name: {{ include "aurora-proto.fullname" . }}
  labels:
    {{- include "aurora-proto.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    {{- include "aurora-proto.selectorLabels" . | nindent 4 }}
